// Alias for the GA4 OAuth callback. This path (/auth/callback) is what's
// registered as the authorized redirect URI on the Google OAuth client.
export { GET } from "../../api/ga4/oauth-callback/route";
